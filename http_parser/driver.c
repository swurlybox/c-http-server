#include "driver.h"

/* read the request, determine which request it was and if it was valid, 
 * then offload execution to a specific response handler. */
void http_parser_main(int client_sock, void *buffer, int buflen) {
    return;
}
